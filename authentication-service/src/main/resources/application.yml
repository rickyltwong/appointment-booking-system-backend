spring:
  application:
    name:
      authentication-service

  datasource:
    url: jdbc:mysql://localhost:3306/j2ee2-final-project
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
